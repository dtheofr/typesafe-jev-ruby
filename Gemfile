source "https://rubygems.org"

gemspec

# public_suffix 7.x (via webmock -> addressable) requires Ruby >= 3.2,
# but this gem supports >= 3.1 — cap it for 3.1 compatibility.
gem "public_suffix", "< 7"
